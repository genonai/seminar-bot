# 주간 세미나 봇 인수인계

> 이 봇은 *seminar_bot* 이라는 Slack 앱입니다. Genon AI AI Engineer 팀 주간 세미나(매주 목 14:00) 운영을 자동화합니다.

---

## 한 줄 요약

**진재님이 매주 손으로 하던 세미나 운영 작업 (일정/발표자/토픽/자료배포/연기처리/공지) 을 봇이 알아서 해줍니다.** 새 운영자는 거의 손 안 대도 되고, 가끔 슬래시 명령 몇 개로 상태 점검 / 수동 조정만 하면 됩니다.

---

## 무엇을 자동으로 해주나

매주 자동 일어나는 일 (운영자 개입 0):

| 시점 | 동작 |
|---|---|
| 월 09:00 | "이번 주 목요일 14:00 — 1부 X / 2부 Y, 토픽 ..." 채널 공지 |
| 수 14:00 | 발표 자료(PDF) 자동으로 채널에 배포 (제출된 것들) |
| 수 17:00 | 자료 아직 미제출 발표자에게 DM "/제출 부탁" |
| 목 13:30 | "오늘 14:00 세미나" 채널 안내 |
| 목 16:00 | 세미나 완료 처리 (발표 통계 갱신) |
| 매일 09:00 | 채널 멤버 sync (입퇴장 자동 반영) |
| 매일 10:00 | 다음 주 발표자 토픽 미등록이면 DM 리마인더 |
| 매일 12:00 | 연기 신청 마감(8일 전) 발표자 DM 리마인더 |
| 토 09:00 | 다가올 일정이 2주 이하 남으면 다음 5주 사이클 자동 추첨 + 채널 공지 + 발표자 DM 토픽 요청 |

---

## 발표 멤버 입장에서 (참고용)

멤버는 그냥 봇 DM에 자연어로 보내면 봇이 알아서 처리합니다. 슬래시 단축키도 있어요:

| 명령 | 용도 |
|---|---|
| `/세미나-일정` | 다가올 5주 일정 (본인 차례에 ⭐) |
| `/세미나-연기` | 발표 못 할 사정 — 자연어 멀티턴 대화로 신청 |
| `/세미나-토픽` | 본인 발표 토픽 등록 (DM 에 "내 토픽은 X" 도 OK) |
| `/세미나-선호도` | 평소 회피 날짜/주차/슬롯 (휴가 등) |
| `/제출` | 발표 PDF 자료 제출 → 봇이 VLM 분석 → 채널 배포 + RAG 인입 |
| 봇 DM 자유 메시지 | 자료 관련 질문, 일정 조회 등 ("ChatGPT 자료 요약해줘") |

---

## 운영자 (당신) 가 쓸 명령들

채널 `#ai-engineer-주간세미나` 에서 슬래시로 입력. 모두 운영자 권한 한정.

### 일상 점검
| 명령 | 용도 |
|---|---|
| `/세미나-현황` | 다가올 5주 일정 + 각 슬롯 토픽/자료 등록 상태 한 눈에 (가장 자주 씀) |
| `/어드민-목록` | 현재 운영자 누구누구인지 |

### 풀 / 멤버 관리
| 명령 | 용도 |
|---|---|
| `/세미나-제외 @user` | 채널엔 있지만 발표 안 하는 사람 (관찰자/휴직/게스트) 풀에서 빼기 |
| `/세미나-포함 @user` | 다시 풀에 넣기 |
| `/세미나-제외-목록` | 현재 제외된 멤버 목록 |
| `/어드민-추가 @user` | 운영자 권한 부여 |
| `/어드민-삭제 @user` | 운영자 권한 해제 |

### 주간 운영
| 명령 | 용도 |
|---|---|
| `/세미나-토픽-알림` | 다음 발표자 중 토픽 미등록자에게 DM 안내 발송 |
| `/세미나-안내` | 회차별 운영 안내 (장소 변경, 회식 등) 등록 — 자동 공지에 포함됨 |
| `/세미나-공지` | 임의 메시지를 모든 채널에 게시 (브로드캐스트) |
| `/세미나-배포` | 자료 채널 배포 수동 트리거 (인자 없으면 이번 주, `all` 이면 전체) |

### 비상시
| 명령 | 용도 |
|---|---|
| `/세미나-재추첨` | 현재 사이클 폐기 + 새 5주 추첨. 멤버 풀 큰 변화 있을 때만. |

### 봇 DM 자연어 (위와 동등)
운영자도 봇 DM 에 자연어로 보내면 처리:
- "이번주는 회의실 B로 변경" → 안내 저장
- "노거현은 발표 안 함" → 풀에서 제외
- "다음 발표 누구야?" → 일정 답변
- "ChatGPT 자료 핵심 요점 뭐야?" → RAG 답변
- "회의실 예약은 좀 해줘" → "권한 밖이라 운영자에게 escalate" (자기 자신한테 DM 옴)

---

## 매주 루틴 (제안)

**월요일 출근**: 봇이 09:00 공지 보냈음. 알림 확인 + 이번 주 발표자가 토픽 등록했는지 보고 안 했으면 `/세미나-토픽-알림`.

**화요일**: 진재님(전 운영자) 이전 처리 방식으로 — Google Cal 초대 미리 보내거나 외부 손님 안내.

**수요일**:
- 14:00 — 봇이 제출된 자료 채널에 자동 배포. 슬랙 알림 확인.
- 17:00 — 봇이 미제출 발표자에게 DM. 그 사람들 반응 확인.
- 추가 안내사항 있으면 `/세미나-안내` 또는 봇 DM 으로 ("이번주는 회의실 B로 변경" 등)

**목요일**:
- 13:30 — 봇이 채널 공지. 회의실 가서 13:55 즈음 입장.
- 16:00 — 봇이 자동 완료 마킹. 끝.

**토요일 09:00** (선택): 봇이 사이클 자동 추첨하면 결과 채널에 공지. 그 결과가 이상하면 `/세미나-재추첨` 으로 다시. 토픽 미등록 발표자들에게 DM 가 자동으로 가니까 그것도 확인.

---

## 자주 발생하는 시나리오

### 1. 발표자가 못 한다고 함
- 그 사람이 `/세미나-연기` 슬래시 또는 봇 DM에 "X일 못 합니다" 라고 보냄
- 봇이 멀티턴 대화로 사유/대체 가능일 받음
- 운영자(당신) + 자동 선정된 대체자 둘 다에게 [승인/거절] 버튼 DM 옴
- 양쪽 승인 누르면 schedule 자동 갱신 + 채널 공지

대체자가 거절하면 → 봇이 자동으로 차순위 후보 (cost 함수 기반) 에게 또 DM. 최대 3번 시도 후 운영자에게 escalate.

### 2. 새 사람이 채널에 들어옴
- 1분 안에 자동 sync → DB에 추가
- 다음 사이클 추첨부터 후보로 들어감
- 만약 발표 풀에 넣고 싶지 않으면 → `/세미나-제외 @new_user`

### 3. 누가 채널 떠남
- 자동 sync → is_active=0 처리
- 다음 추첨에서 제외
- 진행 중인 발표 슬롯은 그대로 (수동 처리 필요 — 그 사람한테 위임된 발표를 다른 사람에게 넘기려면 `/세미나-재추첨` 또는 DB 직접 수정)

### 4. 사이클이 망가져 보임 (예: 잘못 추첨됨)
- `/세미나-재추첨` 한 줄 → 현재 사이클 폐기 + 새로 추첨
- ⚠️ 진행 중 defer 신청 있으면 먼저 마무리

### 5. 자료가 너무 일찍 제출됨 (예: 2주 전)
- 봇이 자동으로 "이번 주 가까운 자료 아니라 보류, 수 14:00에 배포할게요" DM 응답
- 운영자 개입 불필요

### 6. 봇이 이상한 응답
- DM 으로 "이상해, 다시 알려줘" 라고 하면 LLM 이 history 보고 정정 시도
- 그래도 안 되면 → 봇 컨테이너 재시작 (아래 인프라 섹션 참고)

---

## 인프라 (어디에 살아있는가)

| 항목 | 위치 |
|---|---|
| 봇 코드 | GitHub: <https://github.com/genonai/seminar-bot> (private) |
| 봇 서버 | 192.168.73.181 (kube-server), `/opt/seminar-bot` |
| 봇 컨테이너 | Docker (`docker compose`), 컨테이너 이름 `seminar-bot` |
| 데이터 | `/opt/seminar-bot/data/` (SQLite DB + ChromaDB + 제출된 PDF + bot.log) |
| Slack 앱 | api.slack.com/apps → "seminar_bot" |
| 외부 LLM | OpenRouter (`LLM_API_KEY` in `.env`) |

### 서버 접근
```bash
ssh kube-server      # ~/.ssh/config 의 alias. 비밀번호 인증.
cd /opt/seminar-bot
```

### 일반 운영 명령
```bash
# 상태 확인
docker compose ps
docker compose logs --tail 50         # 최근 로그
docker compose logs -f                 # 실시간

# 재시작 (.env 갱신 후 등)
docker compose restart

# 코드 갱신 + 재배포
git pull
docker compose up -d --build

# 정지 (절대 자주 쓰면 안 됨)
docker compose down
```

### 데이터 영속성
- `/opt/seminar-bot/data/` 디렉토리가 호스트 mount → 컨테이너 죽거나 재빌드해도 DB/자료/로그 안 사라짐
- 백업: 일주일에 한 번 `cp -r /opt/seminar-bot/data /backup/...` 정도 권장 (cron으로 자동화 가능)

---

## 비용

전부 OpenRouter 호출:
- LLM (`deepseek/deepseek-v3.2`) — DM 분류/응답/메타 추출
- VLM (`google/gemini-2.5-flash`) — 발표 자료 PDF 페이지 분석
- Embedding (`openai/text-embedding-3-small`) — RAG 검색

**대략 한 달 $1-2 수준** (자료 2개/주, DM 100건/주 가정). OpenRouter 계정의 크레딧 잔액 가끔 확인.

크레딧 충전: <https://openrouter.ai/credits>

---

## 비상 연락 / 도움

| 상황 | 대처 |
|---|---|
| 봇이 응답 안 함 | `docker compose logs --tail 50` → 에러 확인. 안 보이면 `docker compose restart`. |
| 슬래시 커맨드가 "app did not respond" | Slack 앱 페이지에서 reinstall 시도. 그래도 안 되면 봇 재시작. |
| LLM API 잔액 부족 | OpenRouter 크레딧 충전 |
| GenOS embedding 사용 (사내 BGE-M3) 으로 갈아타고 싶음 | `.env` 의 `EMBEDDING_API_BASE_URL` / `EMBEDDING_API_KEY` / `EMBEDDING_MODEL` 변경 후 재시작 |
| 코드 변경 필요 | GitHub 에 PR. 또는 진재님 (전 운영자) 에게 문의 |
| DB 백업 / 복원 | `/opt/seminar-bot/data/seminar.db` 파일 자체가 SQLite. `cp` 로 백업, 복원도 동일. |

---

## 운영자 권한 인수인계 절차

새 운영자가 진재님(현 운영자) 대신 모든 알림/승인을 받으려면:

1. 진재님이 `/어드민-추가 @새운영자` 슬래시 한 번
2. (선택) 진재님 권한 빼려면: 진재님이 `/어드민-삭제 @U07GFTZ6LM8` (자기 자신)
   - **단, 마지막 한 명은 제거 불가** — 새 운영자 추가 후에야 본인 빠짐
3. **Primary admin (= 연기 승인 DM 받는 사람)** 은 첫 번째로 등록된 운영자.
   - 진재님이 빠지면 자동으로 새 운영자가 primary 가 됨
   - 또는 명시적으로 `.env` 의 `ADMIN_USER_IDS` 순서 조정 후 재시작

검증:
```bash
/어드민-목록     # 슬랙에서
```

---

## 키 / 시크릿 (보안)

`.env` 파일 (`/opt/seminar-bot/.env`) 에 다음 토큰들 보관됨. 절대 GitHub 에 commit 금지 (`.gitignore` 처리됨):

| 키 | 용도 |
|---|---|
| `SLACK_BOT_TOKEN` | 봇 자체 토큰 |
| `SLACK_APP_TOKEN` | Socket Mode 연결용 |
| `SLACK_SIGNING_SECRET` | 슬랙 요청 검증 |
| `LLM_API_KEY` | OpenRouter (LLM/VLM/Embedding 공용) |
| `API_TOKEN` | 봇 debug HTTP API 인증 |

운영자 교체 시 보안상 권장: 위 모든 토큰을 새로 발급받아 교체 (옛 운영자가 알고있던 토큰 폐기). 시간 들지만 안전.

토큰 재발급 위치:
- Slack 토큰들: api.slack.com/apps → 앱 → OAuth & Permissions / Basic Information
- OpenRouter: <https://openrouter.ai/keys>
- API_TOKEN: 그냥 `openssl rand -hex 32` 새로 생성 후 `.env` 갱신 + 봇 재시작

---

## Phase 2 / 3 (예정, 미구현)

현재 Phase 1 까지 완료. 향후:
- Phase 2: 자료 자동 수집 강화 (Google Slides 링크 / PPT 변환 등)
- Phase 3: 추가 RAG 기능 (semantic deduplication / 사이클 간 토픽 유사도)

별도 트랙이라 새 운영자가 신경 안 써도 됨. 필요할 때 진재님 또는 후속 개발자가 진행.

---

## 추가 자료

| 문서 | 위치 |
|---|---|
| 코드 + README | <https://github.com/genonai/seminar-bot> |
| 봇 commit history | `git log` (변경 이력) |
| Slack 채널 archive | `#ai-engineer-주간세미나` (검색으로 이전 대화 / 공지 추적 가능) |

---

## 한 줄 요약 (다시)

> 거의 손 안 대도 됩니다. 매주 월 09:00 알림 보고 `/세미나-현황` 한 번 치면 충분. 발표자가 연기 신청하면 [승인] 버튼 누르고, 채널 멤버 변동 있으면 `/세미나-제외 @user` 정도. 나머지는 봇이 알아서.

질문 / 인계 미진한 부분 → 진재님 (`<@U07GFTZ6LM8>`) 또는 GitHub Issues.
