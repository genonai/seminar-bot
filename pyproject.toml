[project]
name = "seminar-bot"
version = "0.1.0"
description = "Genon AI 주간 세미나 운영 슬랙 봇"
requires-python = ">=3.11"
dependencies = [
  "slack-bolt>=1.20",
  "apscheduler>=3.10",
  "pydantic>=2.7",
  "python-dotenv>=1.0",
  "openai>=1.50",
  "tzdata>=2024.1",
  "pypdfium2>=4.30",          # PDF → PNG 렌더링, 시스템 poppler 의존성 X
  "weaviate-client>=4.9",     # vector DB
  "httpx>=0.27",              # Slack 파일 다운로드
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0",
  "pytest-cov>=5.0",
  "ruff>=0.6",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py311"
